import { CommText } from "@/src/ui/common/CommText"
import { View } from "react-native"

const Upload = () => {
    return <View>
        <CommText>업로드</CommText>
    </View>
}

export default Upload;